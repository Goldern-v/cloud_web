export const vmwarePrompt: { [key: string]: any } = {
  MAZ_MIDDLE_NAME:
    '长度为2~32个字符，支持使用英文字母、数字、连字符（-）、下划线（_）组合命名，不支持用数字、连字符（-）、下划线（_）作为命名开头，不支持连续连字符（--）',
  DESC: '长度为0-128个字符,支持中文、英文(大小写)、数字,特殊符号（,，.。?？;；、""“”-!！%$￥）,不支持空格和换行'
}
